import { NavLink } from "react-router";

const Navbar = () => {
  return (
    <nav>
      <NavLink to="/" style={{ marginRight: ".5rem" }}>
        useTransition
      </NavLink>
    </nav>
  );
};

export default Navbar;
